# TASK QUEUE

| Task ID | Priority | Status | Classification | Summary |
|---|---|---|---|---|
| TASK-001 | P1 | DONE | feature, architecture | Phase 1: lucid/SINVAUX compiler core + full desktop scaffold — verified 2026-08-11 |
| TASK-002 | P1 | DONE | feature | Real English NLP layer for intent/requirement extraction (tokenization, grammar, negation, synonym normalization) — deterministic, offline, no AI API — verified 2026-08-11 |
| TASK-003 | P1 | DONE | feature | Add TECHNICAL specialist as second real specialist, registered via SPECIALIST_REGISTRY, run alongside ARCHITECT in the pipeline — verified 2026-08-11 |
| TASK-004 | P1 | DONE | feature | Add UX specialist as third real specialist (user flow, interaction hierarchy, accessibility, usability, information architecture, edge cases), wired into the pipeline alongside ARCHITECT + TECHNICAL — verified 2026-08-11 |
| TASK-005 | P1 | DONE | feature | Real critique + conflict-detection engine replacing critiquePassthrough/conflictPassthrough no-op stages — detects contradictions/tensions across ARCHITECT/TECHNICAL/UX decisions — verified 2026-08-11 |
| TASK-006 | P2 | DONE | bug | Fix game domain KEYWORDS bare "multiplayer" overlap causing non-game inputs to misclassify as game domain — verified 2026-08-11 |
| TASK-032 | P2 | DONE | feature | Add "Video Generation" domain (AI video generation/production prompts — duration, pacing, style, transitions, audio sync), API-free like image-generation — 191/191 tests passing |
| TASK-007 | P1 | DONE | feature | Add SECURITY specialist as fourth real specialist (privacy, auth, authorization, data exposure, unsafe assumptions), wired into pipeline before critique/conflict — independently re-verified 2026-08-11, including one added conflict rule (web: no-login-required vs. SECURITY unsafe-assumptions/UX onboarding) found during verification |
| TASK-008 | P1 | DONE | feature | Add QUICK mode — fast single-pass pipeline (intentAnalysis/domainDetection/requirementExtraction/synthesis/outputValidation only; skips ambiguityDetection, all specialists, critique/conflict), plus desktop UI mode toggle — independently re-verified 2026-08-11 (adversarial pass over the untested desktop UI wiring and SQLite `mode` column; no bugs found, all 8 acceptance criteria genuinely PASS) |
| TASK-009 | P1 | DONE | feature, architecture | Build MASTER mode — multi-round deliberation pipeline (requirementExpansion + 4 specialists + loop-until-dry critique/conflict/alternativeGeneration/decisionScoring, maxRounds cap) mapped onto the brief's 9-round structure, plus a minimal loop-safety fix to critique.ts's Rule 1 (never critique the deliberation loop's own prior output) — independently re-verified 2026-08-11 (adversarial pass: 92/92 tests re-run and 21 new tests read in full, stress-tested the loop with a long contradictory input at maxRounds:5 (~112ms, no hang), confirmed maxRounds:1 hard cap and loop-until-dry both hold, confirmed no auto-resolution and no schema-field invention, confirmed desktop MASTER toggle + SQLite mode column wiring; no bugs found, all 9 acceptance criteria genuinely PASS) |
| TASK-010 | P1 | DONE | feature | Add CREATIVE specialist as fifth real specialist (visual direction, originality, consistency, composition, style, creative opportunities), wired into BOTH ARCHITECT_MODE_PIPELINE and runMasterPipeline's round-2 block — independently re-verified 2026-08-11 (adversarial pass over both dual-pipeline wiring sites and the loop-safety interaction with a 5th specialist; no bugs found, all 9 acceptance criteria genuinely PASS) |
| TASK-011 | P1 | DONE | rebrand | Rename app to "Scaffold" (window title, package/product names, UI header) while keeping SINVAUX as company brand (footer/about) — independently re-verified 2026-08-11 (adversarial pass; all 5 requirements PASS, no bugs found) |
| TASK-012 | P1 | DONE | design, feature | Reorganize desktop UI information density/hierarchy — group by source, collapse secondary categories, distinguish conflicts, add summary strip, progressive-disclosure metadata — independently re-verified 2026-08-11 (adversarial pass: read all 5 UI files line-by-line, grepped for doubled-entity/hex-color artifacts, found and fixed one real bug — a hardcoded `#b5461f` literal in DecisionsPanel.tsx instead of a `--sv-*` token, now `--sv-alert`; traced grouping/collapsing/conflict-exclusion logic by hand against real MASTER-mode test fixtures, confirmed no double-counting/empty-group rendering/information loss; tsc/vite build/vitest all re-confirmed clean; all 11 requirements PASS; no screenshot tool available, restated as an honest known limitation) |
| TASK-013 | P1 | DONE | feature | Add QA specialist as sixth real specialist (break the spec, missing requirements, contradictions, acceptance criteria, test cases, failure states), wired into BOTH ARCHITECT_MODE_PIPELINE and runMasterPipeline's round-2 block — independently re-verified 2026-08-11 (adversarial pass: 105/105 tests re-run, both ARCHITECT and MASTER six-way merges reproduced against real entry points, dual-pipeline wiring and LOOP_STAGE_SOURCES exclusion confirmed, MASTER-loop safety re-stress-tested; no bugs found, all 9 acceptance criteria genuinely PASS) |
| TASK-014 | P1 | DONE | feature | Add CONSTRAINT specialist as seventh and final specialist from the original brief (impossible/high-risk combinations, conflicting requirements, assumption identification, hard-requirement vs preference distinction), wired into BOTH ARCHITECT_MODE_PIPELINE and runMasterPipeline's round-2 block — closes out the full 7-specialist roster — independently re-verified 2026-08-11 (adversarial pass: 114/114 tests re-run and all 9 new tests read in full including the reclassification-no-mutation and negative-control tests; confirmed CONSTRAINT is structurally/conceptually distinct from conflict.ts (raw-input/requirements-pool infeasibility vs. cross-specialist-decision contradiction — not a redundant duplicate); confirmed dual-pipeline wiring (last specialist slot before critique/conflict in both ARCHITECT and MASTER) and 'constraint-specialist' correctly absent from critique.ts's LOOP_STAGE_SOURCES; confirmed append-only provenance (no mutation, grep-clean); confirmed word-boundary-safe triggerA/triggerB pairs in all 3 domains; independently reproduced all-7-sources via real compileArchitect/compileMaster calls and re-stress-tested MASTER-loop safety at maxRounds:5 with a long contradictory input (~144ms, no hang) — no bugs found, all 12 requirements genuinely PASS) |
| TASK-015 | P1 | VERIFIED | feature | Added in-app "Check for Updates" via Tauri updater plugin (`tauri-plugin-updater` + `tauri-plugin-process`) wired to GitHub Releases (private repo YVNGVOU/scaffold-app) for both check and manifest hosting; manual-trigger button only (no background/startup check). Generated updater signing keypair (private key gitignored, never committed), configured `pubkey`/`endpoints` in `tauri.conf.json`, built + signed a real Windows NSIS installer, and published `v0.1.0` to GitHub Releases with `latest.json` — full end-to-end mechanism verified working. See `docs/RELEASING.md` for the repeatable release process and `state/CANONICAL_STATE.md` for key-location/config documentation. Independently re-verified 2026-08-11: private key content confirmed absent from full local git history (`--all --full-history`) and from `origin/master` remote tree; `.gitignore` pattern confirmed effective via `git check-ignore -v`; `tsc --noEmit`/`vite build`/`cargo build` clean; vitest 114/114 (zero compiler/schema diff); `gh release view v0.1.0` confirms 3 assets published. All 10 requirements PASS. |
| TASK-016 | P2 | DONE | design | Make SINVAUX brand credit a more prominent/persistent stamp across the app, not just the left-nav footer — added a fixed-position bottom-right corner stamp visible from every pane, kept the existing left-nav footer credit too; tsc/vite build clean, committed & pushed (a335e05) |
| TASK-017 | P1 | VERIFIED | feature | Interactive clarifying-questions flow — unresolved ambiguities become answerable questions; answers merge into the compiled prompt so the final handoff is actually complete. Added `mergeAnswer(compiled, unresolvedItem, answerText)` in `packages/compiler/src/mergeAnswer.ts` (exported from `index.ts`), pure/headless, append-only (appends a new `kind:'user'`/`source:'user-answered-question'`/`confidence:1.0`/`status:'accepted'` item into `userRequirements`, evidence referencing the original unresolved item's text, original left untouched). `DecisionsPanel.tsx` gets an inline free-text input + "Answer" button per unresolved item (`UnresolvedItemCard`); on submit calls `mergeAnswer` and lifts the updated `CompiledPrompt` to `App.tsx` via new `onAnswered` prop. Answered items (detected via `isAnswered()` matching evidence) drop out of the "Unresolved" section/count into a collapsed "Answered" section — SummaryStrip's unresolved count decreases live and its burgundy styling already cleared at 0 (pre-existing conditional, TASK-012). `App.tsx`'s new `handleAnswered()` updates `compiled` state and persists via the existing `saveCompile` SQLite call (same append-only `compiles`-table pattern as a normal compile) — does not re-run `runArchitectPipeline`/`runQuickPipeline`/`runMasterPipeline`. 4 new tests in `packages/compiler/test/mergeAnswer.test.ts` (no mutation of original/input, multi-answer no-collision, evidence references original text, returns new object). Zero AI API calls (pure data transform). Verified: vitest 118/118 (114 pre-existing + 4 new, zero regressions), `tsc --noEmit` clean on schema/compiler/desktop, `vite build` clean; no Rust files touched so `cargo build` not required for this task. |
| TASK-018 | P1 | VERIFIED | feature | Settings panel (default mode, MASTER maxRounds, About/version/GitHub link, moved "Check for Updates" in) + prompt management (rename/delete/search) + export options (copy/Markdown/`.md`/`.json`) + keyboard shortcuts (Ctrl/Cmd+Enter compile, Ctrl/Cmd+Shift+N new prompt, Esc cancel) — round out the app so it feels like a real, complete product. New `settings` SQLite key/value table + `rename_prompt`/`delete_prompt`/`get_setting`/`set_setting` Tauri commands; new `formatAsMarkdown()` pure helper in `packages/compiler` (5 new tests). Independently re-verified 2026-08-11 (adversarial pass: 123/123 tests re-run, `tsc`/`vite build`/`cargo build` all re-run clean; traced `delete_prompt`'s two-statement child-then-parent delete directly in `commands.rs` and confirmed no orphaned `compiles` rows and matching `Result<T,String>` error convention; traced Settings→App.tsx→`runMasterPipeline({ maxRounds })` wiring end to end and confirmed genuine, not a stub; confirmed export toolbar gated on non-null `compiled` and using real Tauri plugin calls; confirmed keyboard shortcuts don't interfere with normal typing; grepped for entity artifacts/hardcoded hex/network calls — none found; confirmed `scaffold-updater.key` untouched. One minor non-blocking finding: `delete_prompt`'s two `DELETE` statements aren't wrapped in an explicit SQL transaction — order is correct (children before parent) so no orphan risk today, but a crash between statements would leave a torn state; worth a follow-up if strict atomicity is desired). |
| TASK-019 | P1 | VERIFIED | feature, architecture | SINVAUX account login via Supabase Auth (email/password + magic link + `signInWithOtp`), gating `App.tsx` behind a new `AuthGate` wrapper in `main.tsx`, 7-day offline grace period reusing the existing `get_setting`/`set_setting` settings table (`auth_last_verified_at` key), Sign Out added to SettingsPanel. Auth only — no payment/subscription/plan enforcement (explicitly deferred, billing processor TBD). Independently re-verified 2026-08-11 (adversarial pass): anon key independently decoded (`"role":"anon"`, not `service_role`) — credential-safety PASS; `looksLikeExplicitInvalidSession`/7-day-grace classification logic traced and sound, `Date.now() - last < GRACE_PERIOD_MS` correctly strict (no off-by-one); `AuthGate` confirmed to render a "Checking account…" state before any children — no unauthenticated-content flash; Sign Out confirmed to clear both `supabase.auth.signOut()` and `auth_last_verified_at` via `setSetting`; `git diff --stat` confirmed zero changes to `packages/compiler`/`packages/schema` and to `scaffold-updater.key`; vitest 123/123 re-run, `tsc --noEmit` clean on all 3 packages, `vite build` clean, `cargo build` clean (no-op, no Rust files touched); grepped for stray `&amp;amp;` entities, hardcoded hex colors, and stripe/billing/subscription code — none found (one incidental `sub.subscription.unsubscribe()` SDK call, not billing logic). All 13 requirements PASS. No bugs found requiring a fix. |
| TASK-020 | P2 | DONE | feature | Compile version history / diff view — pure `formatDiff(a, b)` helper in `packages/compiler` (set-of-`text` diff per requirement category, top-level scalar field changes, architecture notes matched by `component`, 6 new tests), exported from `packages/compiler/src/index.ts`. New `apps/desktop/src/components/VersionHistory.tsx` modal (opened via a "History" button next to Compile in `App.tsx`, gated on an active prompt) lists a prompt's saved compiles via the existing `listCompiles` command and renders `formatDiff`'s structured output for a selected From/To pair — read-only, no revert/merge. vitest 129/129 (123 pre-existing + 6 new, zero regressions), `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-021 | P2 | DONE | feature | Prompt-target output profiles — `formatAsMarkdown(compiled, profile?)` in `packages/compiler` extended with a new exported `PromptProfile` union (`generic`/`claude`/`chatgpt`/`coding-agent`/`image-model`), each a purely presentational Markdown convention (title, optional framing line, section order/labels) with zero effect on underlying `CompiledPrompt` data and zero AI/network calls; 7 new tests. `apps/desktop/src/components/CompiledOutput.tsx`'s `ExportToolbar` gets a profile `<select>` (default Generic AI, `--sv-*` tokens only) feeding "Copy Markdown"/"Export .md"; "Export .json" unaffected (always raw data). vitest 136/136 (129 pre-existing + 7 new, zero regressions), `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-022 | P3 | DONE | feature | Starter templates per domain — new `apps/desktop/src/starterPrompts.ts` (pure static data, 9 example one-liners: 3 web, 3 game, 3 branding). `App.tsx`'s center pane renders them as clickable `--sv-*`-styled chips directly under the raw-input textarea, shown only while the textarea is empty; clicking a chip calls `setRawInput(text)` and the panel disappears once input is non-empty. Pure frontend, no compiler/schema changes, no domain-detection wiring. vitest 136/136 unchanged (no new compiler tests needed), `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-023 | P3 | DONE | feature | Auto-save draft input — `App.tsx` debounced (~500ms) `useEffect` persists `rawInput` to the existing `settings` table via `get_setting`/`set_setting` (TASK-018), keyed per-prompt (`draft_prompt_<id>`) or `draft_new_prompt` for an unsaved prompt. `handleSelectPrompt`/`handleNewPrompt` (now async) restore the saved draft in preference to the prompt's last-compiled `raw_input`; `handleCompile` clears the relevant draft key(s) after a successful save so a stale draft can't shadow future edits. No new SQLite table/Tauri command, no compiler/schema changes. vitest 136/136 unchanged, `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-024 | P3 | DONE | feature | Global search across all prompts — extended TASK-018's `PromptList.tsx` search: the existing `filtered` `useMemo` now matches `p.raw_input.toLowerCase().includes(q)` in addition to the pre-existing `p.title.toLowerCase().includes(q)` check, purely client-side over the already-loaded `prompts` array (no new backend query/command/column). Placeholder text updated to "Search prompts (title or content)…". No compiler/schema changes needed (no new pure-function helper — plain array filter). vitest 136/136 unchanged, `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-025 | P3 | DONE | feature | Duplicate/fork a prompt — new "Duplicate" button per row in `PromptList.tsx`'s `PromptRow`, reusing the existing `createPrompt(title, rawInput)` command (no new backend command). Creates a new prompt titled `"<original> (copy)"` with the same `raw_input`, no compile history copied, and selects it as the active prompt immediately via the existing `onSelect`/`onChanged` props. Pure UI wiring over an existing command — no compiler/schema changes needed. vitest 136/136 unchanged, `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-026 | P3 | DONE | feature | Onboarding / first-run walkthrough — new `apps/desktop/src/components/OnboardingPanel.tsx`, a one-time dismissible overlay explaining ARCHITECT/QUICK/MASTER modes and the basic input-to-output flow (a few sentences, single "Got it" dismiss action, not a multi-step tour). Shown on mount in `App.tsx` unless `getSetting('onboarding_seen')` already returns `'1'`; dismiss calls `setSetting('onboarding_seen', '1')` via TASK-018's existing settings table — no new SQLite table/Tauri command. Pure UI wiring, no compiler/schema changes needed. vitest 136/136 unchanged, `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-027 | P3 | DONE | feature | Friendlier error states — new `apps/desktop/src/lib/friendlyError.ts` (`toFriendlyError(e)`, pure classification: network/Supabase-unreachable, SQLite/local-database failure, generic fallback, each with a short human summary + untouched raw text) and `apps/desktop/src/components/ErrorNote.tsx` (renders the summary plus a `<details>` "Details" disclosure holding the raw text, `--sv-*` tokens only). All 4 `setError(String(e))` call sites in `App.tsx` (`refreshPrompts`/`handleSelectPrompt`/`handleAnswered`/`handleCompile`) and `LoginScreen.tsx`'s `friendlyError()`/validation messages switched to the same `{ summary, raw }` shape and `ErrorNote` rendering. `AuthGate.tsx` audited — its offline banner was already friendly copy with no raw-error surface, left unchanged. No `packages/compiler`/`packages/schema` change needed (classification helper is pure UI-local logic). vitest 136/136 unchanged, `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-028 | P3 | DONE | feature | Hover tooltip for decision provenance — new `apps/desktop/src/components/ProvenanceTip.tsx`, a pure-CSS hover/focus tooltip (`.sv-tip`/`.sv-tip-bubble` in `theme.css`, `--sv-*` tokens only, no rounded corners) showing a one-line `source · confidence% [· risk]` summary. Wired around `KindTag` in `DecisionsPanel.tsx`'s `RequirementCard` and `UnresolvedItemCard`, alongside (not replacing) the existing click-to-expand `MetaDisclosure` detail — `MetaDisclosure.tsx` itself unchanged. No `packages/compiler`/`packages/schema` change needed (pure UI feature, no new pure-function helper). vitest 136/136 unchanged, `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| VERIFY-020-029 | P1 | DONE | verification | Holistic cross-batch final verification of TASK-020 through TASK-029 (2026-08-12) — fresh full vitest run (136/136), `tsc --noEmit` clean on all 3 packages, `vite build` clean, `cargo build` clean; confirmed 10 real commits with substantive diffs; confirmed `scaffold-updater.key` untouched across the whole batch; grepped whole `apps/desktop/src`/`packages/compiler/src` trees (not just per-implementer files) for entity artifacts/hardcoded hex — none found in batch-touched files; deep-dive on TASK-020's `formatDiff` (set-based text diff hand-verified correct, one documented duplicate-text edge case noted as non-blocking) and TASK-029's undo-on-delete (confirmed closing the app during the 5s undo window cannot leak — `setTimeout` dies with the process, `delete_prompt` never fires, no ambiguous state; confirmed `delete_prompt`'s transaction wrapping from the fca2ec3 fix is still intact). All 10 items PASS. Overall verdict: batch safe to rebuild and ship. See `state/CANONICAL_STATE.md` for full detail. |
| TASK-029 | P3 | DONE | feature | Undo on prompt delete — `apps/desktop/src/components/PromptList.tsx`: delete handling moved from `PromptRow` up to `PromptList` so the confirmed delete is held client-side (`pending` state + `setTimeout`, `UNDO_DELAY_MS` = 5000ms) instead of immediately calling `delete_prompt`; the prompt is optimistically hidden from the list and an "Undo" toast (new `.sv-undo-stack`/`.sv-undo-toast`/`.sv-undo-button` rules in `theme.css`, `--sv-*` tokens only, no rounded corners) appears — clicking Undo clears the timer and restores the row with `delete_prompt` never called; letting the timer elapse fires the existing `delete_prompt` command and `onPromptsChanged()`. Multiple pending deletes tracked independently by id. No `delete_prompt`/Rust changes (existing TASK-018 cascade-delete transaction untouched), no `packages/compiler`/`packages/schema` change. vitest 136/136 unchanged, `tsc --noEmit` clean on all 3 packages, `vite build` clean; no Rust files touched so `cargo build` not required. |
| TASK-031 | P1 | VERIFIED | bug | Fix "Confirm & Recompile" (TASK-030 shipped it buggy — reported: "resets answers and doesnt actually recompile"). Root cause: `handleConfirmRecompile` fed a synthetic augmented-text string through a completely fresh `runArchitectPipeline`/`runQuickPipeline`/`runMasterPipeline`, discarding the prior compile's answered items and re-running domain/ambiguity detection against phrasing it didn't reliably recognize. Fix: new `resumeAndRecompile(compiled, rawInput, mode, options?)` + `reconstructStateFromCompiled(compiled, rawInput)` in `packages/compiler/src/resumeAndRecompile.ts` (exported from `index.ts`) reconstructs a `PipelineState` directly from the existing `CompiledPrompt` — flattens category arrays back into `requirements`/`requirementCategories` (preserving every answered `kind:'user'` item), routes still-`kind:'unresolved'` items into `ambiguities` (not requirements), reuses `compiled.domain`/`compiled.architecture` verbatim (never re-derived) — WITHOUT re-running `intentAnalysis`/`domainDetection`/`requirementExtraction`/`ambiguityDetection`, then genuinely re-runs the 7 specialists + critique/conflict (or MASTER's full loop, mirroring `runMasterPipeline`'s own dry-round logic) + synthesis + outputValidation fresh so they react to the fuller state. `App.tsx`'s `handleConfirmRecompile` rewritten to call `resumeAndRecompile` directly with its own stepper-animation/`saveCompile` persistence (no longer delegates to `handleCompile`, since the resume path's real stage sequence differs from a from-scratch run — tracked via new `isRecompiling`/`recompileStageNames` state so the stepper renders the genuine executed sequence). `buildAugmentedInput` (TASK-030) confirmed to have no other callers and removed as dead code, along with its test file. 7 new tests in `packages/compiler/test/resumeAndRecompile.test.ts`: answered items preserved end-to-end through a real `runArchitectPipeline` → `mergeAnswer` → `resumeAndRecompile` chain; `reconstructStateFromCompiled` correctly routes `kind:'unresolved'` to `ambiguities` not `requirements`; domain/prior architecture notes preserved even when a fresh domain-detection run on the given raw input would pick a different domain; CONSTRAINT specialist's real hard-requirement-language reclassification rule genuinely fires differently against a soft vs. hard-worded preference item (proving specialists actually react to the reconstructed requirements pool, not just unchanged raw input); QUICK mode resume mirrors `runQuickPipeline`'s stage skip; MASTER mode resume runs the deliberation loop; confirms the 4 excluded stage names never appear in `stagesRun`. vitest 143/143 (142 prior − 6 removed `buildAugmentedInput` tests + 7 new). `tsc --noEmit` clean on all 3 packages, `vite build` clean, `cargo build` clean (no Rust touched). Zero AI API calls. This replaces TASK-030's recompile mechanism entirely — TASK-030's Part B (Export Project Folder) is untouched and still correct. |
| TASK-032 | P2 | DONE | feature | Add "graphic-design" domain (posters, print, digital graphics — layout/typography/color theory/print specs, distinct from branding/logo work): `graphic-design` added to `DomainId` union in `packages/schema/src/index.ts`; new `packages/compiler/src/domains/graphic-design/index.ts` DomainModule (word-boundary-safe keyword scoring, 4 default requirements, 4 ambiguity fields, 6-component architecture template, 6 technical/5 UX/4 security/5 creative/5 QA considerations, 2 constraint considerations); registered in `packages/compiler/src/domains/index.ts`; new `packages/compiler/test/domain-graphic-design.test.ts` (5 tests: classification, 2 negative controls, technical specialist output, architect template). vitest 181/181 (176 prior + 5 new), `tsc --noEmit` clean on schema and compiler. |
| TASK-030 | P1 | VERIFIED | feature | Confirm & Recompile + Export Project Handoff Folder. Part A: new pure `buildAugmentedInput(rawInput, answeredItems)` helper in `packages/compiler` (exported from `index.ts`, 6 new tests) appends each `user-answered-question` item's original question + answer as a plain-English sentence to the raw input; `App.tsx`'s `handleCompile` refactored to accept an optional `overrideRawInput` so the normal Compile button and the new `handleConfirmRecompile` share one code path (same pipeline stepper animation, same `saveCompile` persistence — a genuine full pipeline re-run of whichever mode is selected, not a merge-only shortcut); a "Confirm & Recompile (N answered)" button in `DecisionsPanel.tsx` appears once at least one answered item exists, reusing the existing `isAnswered()`. Part B: "Export Project Folder" button in `CompiledOutput.tsx`'s `ExportToolbar` uses the existing `@tauri-apps/plugin-dialog` `open({directory:true})` and `@tauri-apps/plugin-fs` `mkdir`/`writeTextFile` (new `fs:allow-mkdir` capability added to `capabilities/default.json`, scoped to the same `main` window as pre-existing fs permissions) to write `prompt.md` (via `formatAsMarkdown`, respecting TASK-021's profile selector), `prompt.json`, and `README.md` into a sanitized-title subfolder; gated on `compiled` non-null, cancel is a no-op, write failures surface via TASK-027's `toFriendlyError`/`ErrorNote`. vitest 142/142 (136 prior + 6 new), `tsc --noEmit` clean on all 3 packages, `vite build` clean, `cargo build` clean. **Independently re-verified 2026-08-12**: adversarial testing of `sanitizeFolderName()` found and fixed a real path-traversal bug — a title of exactly `".."` (or any dot-only string) survived sanitization unchanged, causing the export to write into the **parent directory** of the user-chosen folder; fixed by rejecting dot-only results the same way empty results are already rejected. All other 16 requirements re-confirmed PASS on fresh re-run (142/142 tests, tsc/vite/cargo all clean, no entity/hex/network artifacts, `scaffold-updater.key` untouched). See `state/CANONICAL_STATE.md` for full requirement-by-requirement detail. |
| TASK-032 | P2 | DONE | feature, domain-knowledge | Added "Software Development" domain (`software-development`) — general-purpose backend/API/CLI/library/systems programming, distinct from `web`. Extended `DomainId` union in `packages/schema/src/index.ts`; new `packages/compiler/src/domains/software-development/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern, 4 default requirements, 4 ambiguity fields (language/runtime, interface type, persistence, deployment target), 6-component architecture template, 3-6 entries each for technical/UX/security/creative/QA considerations, 2 constraint infeasibility pairs); registered in `packages/compiler/src/domains/index.ts` (additive, existing `web`/`game`/`branding` modules untouched). New `packages/compiler/test/domain-software-development.test.ts` (4 tests: real classification, negative control against web, word-boundary regression, architect-output sanity check via real `compileArchitect`). vitest 147/147 (143 prior + 4 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
| TASK-033 | P2 | DONE | feature, domain-knowledge | Added "Mobile Development" domain (`mobile-development`) — native/cross-platform iOS/Android apps: app store considerations, device permissions, offline behavior, push notifications. (Note: originally requested as "TASK-032"; renumbered to TASK-033 since TASK-032 was already claimed by the concurrently-run "Software Development" domain task in this same batch.) Extended `DomainId` union in `packages/schema/src/index.ts` additively (`... | 'mobile-development'`). New `packages/compiler/src/domains/mobile-development/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern — ios/android/swift/kotlin/react native/flutter/app store/play store/push notification/etc.; 4 default requirements; 4 ambiguity fields: platform target, distribution channel, offline behavior, device permissions; 6-component architecture template: app shell/navigation → UI layer/local storage → backend integration → push notification service → app store release pipeline; 4-6 entries each for technical/UX/security/creative/QA considerations; 2 constraint infeasibility pairs: short timeline vs. app store review delay, fully-offline requirement vs. real-time sync). Registered additively in `packages/compiler/src/domains/index.ts` (existing `web`/`game`/`branding`/`software-development` modules untouched). New `packages/compiler/test/domain-mobile-development.test.ts` (4 tests: real classification on a native iOS/Android request, negative control against a web request, word-boundary regression ("multiplayer"/"happy" do not falsely trigger), architect-output sanity check via real `compileArchitect` call). vitest 151/151 (147 prior + 4 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
| TASK-034 | P2 | DONE | feature, domain-knowledge | Added "Desktop Development" domain (`desktop-development`) — native/cross-platform desktop apps: install/update mechanisms, OS-specific behavior, local file access, packaging. (Note: originally requested as "TASK-032"; renumbered to TASK-034 since TASK-032 and TASK-033 were already claimed by concurrently-run domain tasks in this same batch.) Extended `DomainId` union in `packages/schema/src/index.ts` additively (`... | 'desktop-development'`). New `packages/compiler/src/domains/desktop-development/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern — electron/tauri/wpf/win32/system tray/installer/msi/dmg/auto-updater/code signing/etc.; 4 default requirements; 4 ambiguity fields: target platforms, update mechanism, local file access, packaging/distribution; 6-component architecture template: application shell → core application logic → local storage/OS integration → packaging pipeline → code signing → auto-update service; 4-6 entries each for technical/UX/security/creative/QA considerations; 2 constraint infeasibility pairs: short timeline vs. full cross-platform scope, no-installer vs. auto-update requirement). Registered additively in `packages/compiler/src/domains/index.ts` (existing `web`/`game`/`branding`/`software-development`/`mobile-development` modules untouched). New `packages/compiler/test/domain-desktop-development.test.ts` (4 tests: real classification on a native cross-platform desktop request, negative control against a web request, word-boundary regression ("betray"/"happy" do not falsely trigger), architect-output sanity check via real `compileArchitect` call). vitest 155/155 (151 prior + 4 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
| TASK-035 | P2 | DONE | feature, domain-knowledge | Added "Unity" domain (`unity`) — Unity game engine specifics: C# scripting, prefabs, scene management, asset pipeline, build targets, physics/rendering settings. Distinct from the generic `game` domain (which still lists "unity" as one of many engine keywords for plain "make a game in Unity" requests); this domain wins classification only when genuine Unity-workflow vocabulary is present (prefabs, MonoBehaviour, scene management, URP/HDRP, Addressables, etc.). (Note: originally requested as "TASK-032"; renumbered to TASK-035 since TASK-032 through TASK-034 were already claimed by domain tasks in prior batches.) Extended `DomainId` union in `packages/schema/src/index.ts` additively (`... | 'unity'`). New `packages/compiler/src/domains/unity/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern — unity3d/prefab/monobehaviour/gameobject/scriptableobject/scene management/asset pipeline/build target/urp/hdrp/addressables/il2cpp/etc.; 4 default requirements; 4 ambiguity fields: render pipeline, build targets, Unity version, scripting backend; 6-component architecture template: scene hierarchy → prefab library → scripting layer / asset pipeline → physics and rendering config → build configuration; 5 entries each for technical/UX/security/creative/QA considerations; 3 constraint infeasibility pairs: HDRP vs mobile/WebGL target, no-budget vs open-world/DOTS scope, short timeline vs multiplayer networking). Registered additively in `packages/compiler/src/domains/index.ts` (existing `web`/`game`/`branding`/`software-development`/`mobile-development`/`desktop-development` modules untouched). New `packages/compiler/test/domain-unity.test.ts` (6 tests: real classification on a Unity-specific request, negative control against a web request, negative control confirming a generic "game in Unity" request without Unity-specific detail still classifies as `game` not `unity`, word-boundary regression, architect-output sanity check, technical-consideration sanity check — all via real `compileArchitect` calls). vitest 161/161 (155 prior + 6 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
| TASK-036 | P2 | DONE | feature, domain-knowledge | Added "Unreal Engine" domain (`unreal`) — Unreal Engine specifics: Blueprints vs C++, level design, materials/shaders, performance profiling, platform export. (Note: originally requested as "TASK-032"; renumbered to TASK-036 since TASK-032 through TASK-035 were already claimed by domain tasks in prior batches.) Extended `DomainId` union in `packages/schema/src/index.ts` additively (`... | 'unreal'`). New `packages/compiler/src/domains/unreal/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern — blueprint/ue4/ue5/nanite/lumen/niagara/sequencer/umg/gameplay ability system/metahuman/etc.; 4 default requirements; 4 ambiguity fields: Blueprint vs C++, rendering features, target platforms, Unreal version; 6-component architecture template: level layout → gameplay framework → blueprint/C++ split / material and shader library / content pipeline → packaging configuration; 5 entries each for technical/UX/security/creative/QA considerations; 3 constraint infeasibility pairs: Nanite/Lumen vs mobile target, no-budget vs open-world/GAS/MetaHuman scope, short timeline vs multiplayer networking). Registered additively in `packages/compiler/src/domains/index.ts` (existing `web`/`game`/`branding`/`software-development`/`mobile-development`/`desktop-development`/`unity` modules untouched). New `packages/compiler/test/domain-unreal.test.ts` (6 tests: real classification on an Unreal-specific request, negative control against a web request, negative control confirming a generic "game in Unreal" request without Unreal-specific detail still classifies as `game` not `unreal`, word-boundary regression, architect-output sanity check, technical-consideration sanity check — all via real `compileArchitect`/`runArchitectPipeline` calls). vitest 167/167 (161 prior + 6 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
| TASK-037 | P2 | DONE | feature, domain-knowledge | Added "Roblox" domain (`roblox`) — Roblox development specifics: Luau scripting, Roblox Studio, monetization (Robux/game passes/developer products), moderation/content policy, cross-device play. (Note: originally requested as "TASK-032"; renumbered to TASK-037 since TASK-032 through TASK-036 were already claimed by domain tasks in prior batches.) Extended `DomainId` union in `packages/schema/src/index.ts` additively (`... | 'roblox'`). New `packages/compiler/src/domains/roblox/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern — roblox/luau/roblox studio/game pass/robux/datastore/obby/remoteevent/remotefunction/humanoid/etc.; 4 default requirements; 4 ambiguity fields: monetization, genre, platform target, player scale; 6-component architecture template: client scripts → server scripts → RemoteEvents/RemoteFunctions → DataStore layer → game passes/developer products → Roblox Studio project; 4-5 entries each for technical/UX/security/creative/QA considerations; 2 constraint infeasibility pairs: client-side trust vs. exploit-resistance claim, short timeline vs. full monetization/economy scope). Registered additively in `packages/compiler/src/domains/index.ts` (existing `web`/`game`/`branding`/`software-development`/`mobile-development`/`desktop-development`/`unity`/`unreal` modules untouched). New `packages/compiler/test/domain-roblox.test.ts` (4 tests: real classification on a canonical Roblox request, negative control against a web request, negative control against a Unity game request, architect-output sanity check confirming Roblox-appropriate architecture/considerations via real `compileArchitect`/`runArchitectPipeline` calls). vitest 171/171 (167 prior + 4 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
| TASK-038 | P2 | DONE | feature, domain-knowledge | Added "Blender / 3D Modeling" domain (`blender`) — 3D modeling/animation/rendering in Blender: topology, rigging, texturing/materials, render engine choice, export formats for game engines. (Note: originally requested as "TASK-032"; renumbered to TASK-038 since TASK-032 through TASK-037 were already claimed by domain tasks in prior batches.) Extended `DomainId` union in `packages/schema/src/index.ts` additively (`... | 'blender'`). New `packages/compiler/src/domains/blender/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern — blender/topology/retopology/rig/rigging/texture/uv unwrap/shader/cycles/eevee/sculpt/low poly/armature/glTF/etc.; 4 default requirements; 4 ambiguity fields: purpose, topology target, rigging needs, render engine; 6-component architecture template: base mesh/blockout → topology/retopology → UV unwrap → texturing/materials → rigging → render/export; 3-6 entries each for technical/UX/security/creative/QA considerations; 2 constraint infeasibility pairs: real-time budget vs. cinematic detail, render deadline vs. path tracing). Registered additively in `packages/compiler/src/domains/index.ts` (existing `web`/`game`/`branding`/`software-development`/`mobile-development`/`desktop-development`/`unity`/`unreal`/`roblox` modules untouched). New `packages/compiler/test/domain-blender.test.ts` (5 tests: real classification on a canonical Blender request, 2 negative controls against web/game requests, technical-specialist output sanity check, architect-specialist architecture-template check — all via real `compileArchitect`/`runArchitectPipeline` calls). vitest 176/176 (171 prior + 5 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
| TASK-039 | P2 | DONE | feature, domain-knowledge | Added "Image Generation" domain (`image-generation`) — AI image generation prompts/workflows: style/composition direction, aspect ratio, resolution, iteration/variation strategy. Domain describes what the USER is requesting; Scaffold itself remains deterministic and makes zero AI API calls. (Note: originally requested as "TASK-032"; renumbered to TASK-039 since TASK-032 through TASK-038 were already claimed by domain tasks in prior batches.) Extended `DomainId` union in `packages/schema/src/index.ts` additively (`... | 'image-generation'`). New `packages/compiler/src/domains/image-generation/index.ts` (word-boundary-safe keyword scoring mirroring `web/index.ts`'s pattern — image generation/ai image/text-to-image/midjourney/dall-e/stable diffusion/img2img/inpainting/upscaling/aspect ratio/negative prompt/etc.; 4 default requirements; 4 ambiguity fields: style, aspect ratio/resolution, subject/composition, iteration strategy; 5-component architecture template: prompt specification → style reference → generation parameters → iteration loop → output pipeline; 4-6 entries each for technical/UX/security/creative/QA considerations; 2 constraint infeasibility pairs: photorealistic vs. cartoon/anime style contradiction, print-ready output vs. low-res/thumbnail source). Registered additively in `packages/compiler/src/domains/index.ts` (existing `web`/`game`/`branding`/`software-development`/`mobile-development`/`desktop-development`/`unity`/`unreal`/`roblox`/`blender`/`graphic-design` modules untouched). New `packages/compiler/test/domain-image-generation.test.ts` (5 tests: real classification on a canonical AI image request, 2 negative controls against web/branding requests, technical-specialist output sanity check, architect-specialist architecture-template check — all via real `compileArchitect`/`runArchitectPipeline` calls). vitest 186/186 (181 prior + 5 new), `tsc --noEmit` clean on `packages/schema` and `packages/compiler`. Zero AI API calls. |
